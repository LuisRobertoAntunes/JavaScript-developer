const numeros = [1, 2, 3, 4, 5, 8]

numeros.splice(1, 1) // Exclui elemento do array
console.log(numeros)

numeros.pop() // Remove o ultimo elemento do array
console.log(numeros.pop())
console.log(numeros)
